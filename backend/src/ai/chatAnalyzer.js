/**
 * Mock Chat Analyzer – embeds chat messages for later retrieval
 */
export async function analyzeChat(messages, projectId) {
  // TODO: Embed messages using embedder.js and store in vectorStore.js
  return { summary: 'The team discussed authentication flow and database schema decisions.' };
}
